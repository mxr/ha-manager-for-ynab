pending_income:
  fields:
    for_real:
      required: false
      example: false
      default: false
      selector:
        boolean:
    quiet:
      required: false
      example: false
      default: false
      selector:
        boolean:

sqlite_export:
  fields:
    full_refresh:
      required: false
      example: false
      default: false
      selector:
        boolean:
    quiet:
      required: false
      example: false
      default: false
      selector:
        boolean:
