pending_income:
  fields:
    for_real:
      required: false
      example: false
      default: false
      selector:
        boolean:
    quiet:
      required: false
      example: false
      default: false
      selector:
        boolean:

auto_approve:
  fields:
    for_real:
      required: false
      example: false
      default: false
      selector:
        boolean:
    quiet:
      required: false
      example: false
      default: false
      selector:
        boolean:

sqlite_export:
  fields:
    full_refresh:
      required: false
      example: false
      default: false
      selector:
        boolean:
    quiet:
      required: false
      example: false
      default: false
      selector:
        boolean:

sqlite_query:
  fields:
    sql:
      required: true
      example: SELECT * FROM budgets LIMIT 10
      selector:
        text:
          multiline: true
    output_format:
      required: false
      example: json
      default: json
      selector:
        select:
          options:
          - json
          - csv
